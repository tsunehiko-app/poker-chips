services:
  - type: web
    name: poker-chips
    runtime: node
    buildCommand: |
      cd client && npm install && npm run build &&
      cd ../odds-client && npm install && npm run build &&
      cd ../server && npm install && npm run build
    startCommand: cd server && npm start
    envVars:
      - key: NODE_ENV
        value: production
